# Local parameter overrides — loaded on top of conf/base/parameters.yml
# Example: set data_source.enable_finance: false to skip financial scanning

# 数据库连接串（原 conf/local/credentials.yml 的 duckdb_finance.con）
# 非敏感信息，放这里即可；敏感值走 deploy/local.env
database:
  con: "duckdb:///data/warehouse/finance.duckdb"
