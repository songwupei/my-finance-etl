# ============================================================
# 凭据模板 —— 真实文件 conf/local/credentials.yml 已移出版本控制
# ============================================================
# 背景：仓库代码并不读取 credentials.yml（Kedro 的 credentials 层未被使用），
# 而它曾以明文形式把高德 API key 提交进仓库，因此改为环境变量承载。
#
# 敏感值的归属：
#   高德 API key → deploy/local.env（已 gitignore、权限 600）里的
#                  AMAP_API_KEY_S=...
#                  deploy/lib.sh 会导出给 shell 脚本，
#                  my_finance_shared.config 会在导入时加载给 Python。
#   非敏感连接串 → conf/local/parameters.yml 的 database.con
#
# 若将来确实需要 Kedro 的 credentials 层，把本文件复制成
# credentials.yml（已被 .gitignore 忽略）再填值即可：

amap:
  api_key: "<FILL_ME>"
